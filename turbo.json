{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "format": {},
        "lint": {},
        "dev": {
            "cache": false,
            "persistent": true
        },
        "build": {
            "dependsOn": ["^build"],
            "outputs": [
                ".svelte-kit/**",
                ".vercel/output/**",
                "build/**",
                "dist/**"
            ]
        }
    }
}
